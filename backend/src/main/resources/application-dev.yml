spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jingxuan?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: 252629
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

# 开发环境 DeepSeek 审核失败时放行，不阻塞流程
deepseek:
  fallback: bypass

logging:
  level:
    com.jingxuan: debug
    com.baomidou: debug
