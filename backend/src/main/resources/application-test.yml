spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jingxuan_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

  data:
    redis:
      host: localhost
      port: 6379

# DeepSeek 审核测试环境直接放行
deepseek:
  api-key: ""
  fallback: bypass

logging:
  level:
    com.jingxuan: warn
    com.baomidou: warn
    org.springframework: warn
