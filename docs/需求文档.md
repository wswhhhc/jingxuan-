# 学院作品展示平台 需求文档 V2.0

> **V2.0 更新说明**：基于 V1.9 同步实际项目代码现状。新增功能包括：邮箱验证注册、AI 批量导入用户、点赞与浏览计数、游客评论、排行榜公示控制、评分批次通知、深色模式、运行说明折叠展示等。技术栈对齐当前实现（Vite 8 / Redis 已落地）。数据库表结构同步至最新迁移。

## 一、项目概述

### 1.1 项目背景
某 IT 学院每年会产生大量学生课程作品、实训项目与毕业设计成果。传统的作品提交、展示、评分和汇总主要依赖人工收集与线下管理，存在资料分散、统计困难、展示效果弱、评审效率低等问题。为提升学院作品管理与展示水平，拟开发一套线上作品展示平台，实现作品提交、审核、展示、评分、排行与精选发布的一体化管理。

> **与原始需求的差异说明**：相较学院原始课题描述，本文档新增了"管理员审核"环节。原始需求中学生提交后直接进入展示，增加审核环节的目的是：① 确保展示作品信息完整、附件有效；② 避免不合规内容直接暴露在前台；③ 在实际教学场景中更贴近真实上线流程。此改动已在指导老师处确认。

### 1.2 项目目标
- 提供统一的学生作品提交入口，支持压缩包、截图、演示视频等资料上传
- 支持作品图文展示、视频播放与少量精选作品在线体验
- 建立教师评分与评语机制，支持排行榜统计与展示
- 提供管理员审核、公告发布、LLM 内容安全审核规则配置、日志追踪等后台能力
- 满足课程实训项目的完整性、可演示性与可维护性要求

### 1.3 建设原则
- 以 PC 端为主，优先保证教师评审与后台管理体验
- 以"作品"为核心对象，兼顾个人作品与团队作品
- 采用前后端分离架构，便于分工协作与后续扩展
- 坚持"先完成核心闭环，再实现加分功能"的开发策略

### 1.4 用户角色

| 角色 | 职责 |
|------|------|
| 学生 | 提交作品、补充资料、查看审核结果、查看评分与排行 |
| 教师 | 浏览作品、查看详情、评分、填写评语 |
| 管理员 | 用户管理、作品审核、发布/下线、精选与预览地址维护、内容审核规则管理、公告发布、日志查看 |

---

## 二、业务范围与流程

### 2.1 业务范围
本系统主要服务于学院内部课程作品、实训作品和毕业作品的集中管理与展示，不面向社会化投稿与公开运营。系统重点解决以下问题：
- 学生作品资料统一提交与管理
- 教师线上评分与评语记录
- 优秀作品的集中展示与排行公示
- 少量精选作品的在线体验部署

### 2.2 核心业务流程
1. 管理员导入或创建用户账号，配置基础数据
2. 学生登录系统后填写作品信息并提交作品资料
3. 管理员审核作品内容与附件完整性
4. 审核通过的作品进入教师评审区，教师可进行评分与评语填写
5. 管理员公示排行榜后，系统根据评分规则生成排行榜
6. 管理员从已发布作品中挑选精选作品，配置已部署项目的服务器访问地址
7. 系统审核通过/驳回/排行公示时自动发送站内通知

### 2.3 作品状态与发布状态

作品状态分为"审核状态"和"发布状态"两类，分别对应不同业务职责，避免同一个字段混合表达审核与展示含义。

#### 2.3.1 审核状态流转

| 状态 | 说明 | 可执行角色 |
|------|------|------|
| 草稿 | 学生已创建但未正式提交 | 学生 |
| 已提交 | 学生已提交，等待管理员审核（学生只读，不可编辑） | 管理员 |
| 已驳回 | 管理员审核未通过，需学生修改后重新提交 | 学生、管理员 |
| 已通过 | 管理员审核通过，教师可评分；前台暂不可见 | 管理员、教师 |

说明：
- 学生仅可编辑"草稿""已驳回"状态下的作品
- 学生仅可删除"草稿"状态或"已驳回且未重新提交"状态的作品
- 教师仅对审核状态为"已通过"的作品进行评分；作品是否已发布不影响教师评分权限
- "已通过"表示作品已通过审核，但不等于前台公开展示

#### 2.3.2 发布状态流转

| 状态 | 说明 | 可执行角色 |
|------|------|------|
| 未发布 | 作品审核通过后尚未在前台展示 | 管理员 |
| 已发布 | 管理员手动发布后，作品在前台公开可见 | 管理员、教师、学生 |
| 已下线 | 展示结束或管理员手动下线，前台不可见 | 管理员 |

说明：
- "已通过"与"已发布"的区别：审核通过后作品进入"已通过"状态，此时教师端可见、可评分，但学生前台不可见；管理员执行"发布"操作后，发布状态变为"已发布"，作品才在前台公开展示。两个状态分离的目的是让管理员在审核通过后还有一次展示控制机会（如统一发布时间、暂缓展示等）
- 精选展示不是独立状态，而是在已发布作品上设置 `featured=1` 标记；只有精选作品可配置在线体验地址

---

## 三、功能需求

### 3.1 用户管理模块

| 编号 | 功能 | 描述 |
|------|------|------|
| U-01 | 账号导入/创建 | 管理员可按学号、工号手动创建学生、教师账号，或通过 DeepSeek AI 自然语言描述批量导入用户信息 |
| U-02 | 用户登录 | 用户通过账号密码登录系统 |
| U-03 | 首次登录改密 | 首次登录后需修改初始密码 |
| U-04 | 个人信息维护 | 用户可修改头像、联系方式、密码等信息 |
| U-05 | 角色权限管理 | 管理员可分配角色并控制菜单与接口权限 |
| U-06 | 账号状态管理 | 管理员可启用、禁用账号 |
| U-07 | 邮箱验证注册 | 学生/教师可通过邮箱验证码自助注册账号，学生注册时需选择班级 |

说明：
- 出于校园场景安全性考虑，优先采用"管理员预置账号"的方式，邮箱注册作为补充渠道
- "记住密码"建议调整为"记住登录状态"（已实现）
- **AI 批量导入**：管理员在后台输入自然语言描述（如"23 级软件 1 班张三、李四"），系统调用 DeepSeek API 解析并批量创建用户账号，导入前提供预览确认

### 3.2 作品管理模块

| 编号 | 功能 | 描述 |
|------|------|------|
| W-01 | 创建作品 | 学生创建作品，填写名称、简介、技术栈、指导教师、封面图等信息 |
| W-02 | 附件上传 | 上传截图、演示视频、压缩包等文件，填写运行说明；无需强制上传源代码压缩包 |
| W-03 | 团队成员维护 | 支持维护团队成员信息，标记队长 |
| W-04 | 作品提交 | 学生确认资料完整后提交审核 |
| W-05 | 作品编辑 | 在允许状态下修改作品信息或重新上传附件 |
| W-06 | 作品删除 | 删除未正式进入审核流程的作品 |
| W-07 | 状态查询 | 查看审核状态、驳回原因、发布时间等信息 |

说明：
- 附件支持先上传、后绑定到作品（work_id 允许为空），适应长流程编辑场景
- 源代码压缩包不再作为必填项，附件体系统一为通用文件上传

### 3.3 团队作品规则
- 系统同时支持个人作品与团队作品
- 每个作品只能有 1 名负责人（队长）发起提交
- 队长可维护团队成员名单，成员信息至少包含姓名、学号、班级
- 同一学生在同一评审批次中默认只能归属 1 个作品（仅对已注册账号生效；未注册成员因无法验证身份，不纳入此约束，属于有意的设计折中）
- 排行榜按"作品"维度统计，不按个人维度重复计分

### 3.4 审核与展示模块

| 编号 | 功能 | 描述 |
|------|------|------|
| D-01 | 作品审核 | 管理员审核作品信息与资料完整性，可通过或驳回 |
| D-02 | 作品列表 | 按班级、技术栈、提交时间、状态等条件筛选浏览，支持关键词搜索 |
| D-03 | 作品详情 | 展示作品名称、团队信息、简介、技术栈、截图、视频、运行说明、点赞数、浏览数 |
| D-04 | 视频播放 | 支持在线播放演示视频 |
| D-05 | 作品搜索 | 按作品名称、学生姓名、技术栈等关键词搜索 |
| D-06 | 前台发布 | 审核通过后由管理员控制是否公开展示 |
| D-07 | 作品下线 | 管理员将已发布作品下线，下线后前台不可见 |
| D-08 | 运行说明查看 | 作品详情页展示运行说明，内容超出面板高度时折叠，支持点击展开查看完整内容 |
| D-09 | 作品点赞 | 登录用户可对作品点赞，实时更新点赞计数 |

说明：
- 教师评分页面采用匿名评审模式，仅展示作品名称、简介、技术栈、截图、视频等信息
- 前台公开展示页和管理员详情页可展示团队成员信息

### 3.5 评分与评论模块

| 编号 | 功能 | 描述 |
|------|------|------|
| S-01 | 教师评分 | 教师对作品按多个维度打分 |
| S-02 | 教师评语 | 教师填写评语，支持在截止前修改 |
| S-03 | 评分记录查询 | 管理员可查看评分明细与汇总结果 |
| S-04 | 评论互动 | 学生、教师和游客可在作品下方进行交流评论，支持回复功能 |
| S-05 | LLM 内容安全审核 | 对评语、评论、作品简介等文本进行内容安全检测，调用 DeepSeek 官方 API（当前配置模型 `deepseek-chat`）做 LLM 内容审核，识别色情、暴力、辱骂、政治敏感、广告引流等违规内容 |
| S-06 | 检测规则管理 | 管理员可维护审核 prompt 模板、违规类别开关、检测失败时的处理策略（拒绝/警告/人工复核） |
| S-07 | 评分批次管理 | 管理员创建评分批次，设置评分起止时间、适用班级范围；批次过期后教师不可再提交或修改评分 |

说明：
- 游客评论时需填写昵称，评论内容同样经过 LLM 内容安全审核
- 评论支持嵌套回复（通过 parent_id 实现）
- 删除评论时其下所有回复一并删除

**内容安全审核实现方案（LLM 方式）：**

调用链路：
```
用户提交文本 → 后端收到请求
  → 调用 DeepSeek 官方 API（base_url: https://api.deepseek.com，path: /chat/completions）
  → 模型: deepseek-chat
  → system prompt 定义审核规则（违规类别、输出格式）
  → 设置 response_format: {"type":"json_object"}
  → 返回 JSON: {"passed": true/false, "category": "违规类别", "reason": "具体原因"}
  → passed=true → 正常入库
  → passed=false → 拒绝提交，将原因返回前端提示用户修改
```

结合 DFA 敏感词过滤器作为辅助手段，DFA 基于 Trie 树实现 O(n) 匹配，覆盖高置信度敏感词（辱骂、色情、暴力、政治敏感、广告引流），边缘情况交由 AI 审核处理。

注意事项：
- 每次检测有 1-3 秒网络延迟，前端需做 loading 提示
- API 按 token 计费，单次检测约消耗 200-500 token
- 需对 JSON 解析失败、接口超时、API 不可用等情况做兜底处理（重试或进入人工复核）

### 3.6 评分规则
- 评分维度共 4 项，总分 100 分
- 建议评分占比如下：
  - 创新性：25 分
  - 技术难度：25 分
  - 完成度：30 分
  - 实用性：20 分
- 每个教师对同一作品只能提交 1 次正式评分，但可在评分截止前修改
- 若一个作品由多位教师评分，则最终得分取所有教师总分的平均值，保留两位小数
- 若排行榜分数相同，则依次按"完成度得分""创新性得分""作品提交时间（submit_time）"进行排序，提交时间早者排前
- 评分批次由管理员在后台创建，一个批次可关联多个班级；同一作品若跨批次展示，以最新批次评分为准
- 教师评分时默认隐藏学生姓名与学号，仅展示作品名称与简介（避免人情分）；管理员可查看评分人与被评作品的完整对应关系
- 学生查看分数与排名：在管理员发布排行榜公示之前，学生不可见任何评分信息；排行榜公示后，学生仅可看到自己的分数与排名

### 3.7 在线体验与部署模块

| 编号 | 功能 | 描述 |
|------|------|------|
| P-01 | 精选作品标记与筛选 | 管理员从已发布作品中选取 1 至 2 个标记为精选，作为在线体验对象 |
| P-02 | 预览地址维护 | 管理员为精选作品维护已部署项目的 `previewUrl` 服务器访问地址 |
| P-03 | 在线体验入口 | 前台作品详情页在存在 `previewUrl` 时展示"在线体验"按钮 |
| P-04 | 反向代理配置 | 需要统一域名时，通过 Nginx 将 `/preview/{作品ID}` 代理到实际服务 |
| P-05 | 上线/下线管理 | 管理员通过作品发布、下线和精选开关控制是否展示在线体验入口 |

说明：
- 精选作品数量建议控制在 1 至 2 个，避免部署维护成本过高
- 其余作品统一采用"截图 + 演示视频"的方式展示
- 当前落地方式：管理员配置已部署服务地址，平台不负责自动解压、构建、启动或停止学生项目进程

Nginx 配置示例：

```nginx
location /preview/5 {
    proxy_pass http://127.0.0.1:9003;
    proxy_set_header Host $host;
}
```

### 3.8 排行榜模块

| 编号 | 功能 | 描述 |
|------|------|------|
| R-01 | 综合排行榜 | 按作品最终得分降序排列，默认展示前 10 名 |
| R-02 | 分类排行 | 按技术栈维度筛选生成排行榜 |
| R-03 | 奖项配置 | 管理员可配置一等奖、二等奖、三等奖及优秀奖的名称与名额 |
| R-04 | 奖品管理 | 管理员可为每个奖项配置奖品名称、图片、数量；系统记录奖品发放状态（未发放/已发放） |
| R-05 | 排行公示 | 管理员公示排行榜后，前台公开展示排行榜结果，前 N 名标注获奖等级与对应奖品 |
| R-06 | 数据刷新 | 评分更新后自动或手动刷新排行榜缓存（Redis） |
| R-07 | 批次筛选 | 前台可按评分批次/学期筛选查看历史排行榜，默认展示当前批次 |
| R-08 | 排行榜公示控制 | 管理员可控制排行榜的公示/取消公示；公示前学生对评分和排名不可见 |

说明：
- 排行榜以"评分批次"为维度独立计算，不同批次的排行榜互不干扰
- 排行榜查询结果通过 Redis 缓存加速，缓存键含综合排行和分类排行两种
- 排行榜公示后系统自动向相关学生发送站内通知

### 3.9 评分批次通知模块

| 编号 | 功能 | 描述 |
|------|------|------|
| B-01 | 通知内容配置 | 管理员在创建/编辑评分批次时可填写通知标题和通知内容（作品要求、上传材料说明等） |
| B-02 | 通知发布 | 管理员手动发布通知，系统向批次范围内所有学生发送站内通知 |
| B-03 | 新学生自动补发 | 批次进行中新注册的学生若班级在批次范围内，系统自动为其补发批次通知 |

### 3.10 系统管理模块

| 编号 | 功能 | 描述 |
|------|------|------|
| A-01 | 公告管理 | 管理员发布、编辑、删除系统公告，支持置顶 |
| A-02 | 操作日志 | 记录登录、提交、审核、评分等关键操作，含请求 IP、路径、耗时 |
| A-03 | 数据字典 | 维护班级、技术栈、作品类型等基础数据 |
| A-04 | 审核记录管理 | 查看作品审核历史、驳回原因、审核人信息 |
| A-05 | 消息通知 | 作品状态变更（审核通过/驳回、已发布、已下线）时，系统自动向相关学生发送站内通知；排行榜公示后通知学生查看自己的分数与排名；评分批次通知发布后通知范围内学生 |

通知类型包括：
- `WORK_AUDIT` — 审核结果通知
- `WORK_PUBLISH` — 发布/下线通知
- `SCORE` — 评分相关通知
- `BATCH_NOTICE` — 评分批次通知
- `SYSTEM` — 系统通知

---

## 四、非功能需求

### 4.1 性能要求
- 普通页面在校园网络环境下平均加载时间不超过 3 秒
- 单次上传请求不超过 1600MB，单文件不超过 1536MB；课程验收建议仍控制普通作品压缩包在 200MB 以内，便于备份和演示
- 支持至少 100 人并发访问作品列表、作品详情等常用页面
- 排行榜查询应在 2 秒内返回结果（Redis 缓存命中时 < 100ms）

### 4.2 安全要求
- 用户密码采用 BCrypt 加密存储
- 使用 Spring Security + JWT 完成身份认证与权限控制，JWT 登出后加入黑名单（Caffeine Cache）
- 文件上传需校验扩展名（白名单：zip/rar/7z/jpg/png/gif/mp4/pdf，禁止可执行文件如 exe/sh/bat）、大小、MIME 类型（Hutool FileTypeUtil 魔数双重检测）与存储路径，上传后重命名文件防止路径穿越
- 对常见 SQL 注入、XSS 等风险进行防护
- 对作品简介、评论、评语等文本进行 DFA 敏感词过滤 + LLM 内容安全审核双层检测
- 精选作品在线体验地址应仅允许管理员配置，不允许学生自行指定服务地址
- DeepSeek API Key 通过环境变量配置，不暴露给前端
- 公开 API 端点实施 IP 限流（Caffeine Cache，20 次/秒/IP）
- Nginx 配置 CSP/X-Frame-Options/X-Content-Type-Options 等安全响应头

### 4.3 可用性要求
- 系统界面简洁清晰，支持深色模式切换
- 以 PC 端为主，兼容 1366 x 768 及以上主流分辨率
- 支持 Chrome、Edge、Firefox 等主流浏览器
- 核心功能操作路径清晰，常见操作不超过 3 次点击

### 4.4 可维护性要求
- 后端采用分层架构，便于模块化开发与维护
- 前后端分离，接口遵循 RESTful 风格
- 数据库设计规范，提供完整建表 SQL 脚本与增量迁移
- 关键业务模块应具备基础注释、接口文档与测试数据

---

## 五、技术选型建议

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Spring Boot 3.2.5 / Java 17 | 课程常用框架，生态完善 |
| 权限认证 | Spring Security + JWT（jjwt 0.12.5） | 实现登录认证与 RBAC 权限控制 |
| ORM | MyBatis-Plus 3.5.7 | 提高常规 CRUD 开发效率，雪花 ID + 逻辑删除 |
| 数据库 | MySQL 8.0（utf8mb4） | 存储业务数据 |
| 缓存 | Redis 7（Lettuce） | 用于排行榜缓存、热点数据加速、限流计数器 |
| 文件存储 | 本地文件系统 | 存储压缩包、图片、视频 |
| 前端框架 | Vue 3 + TypeScript + Vite 8 | 按需导入 Element Plus，手动 chunk 拆分 |
| UI | Element Plus 2.14 + Pinia 3 | 管理后台与前台页面组件库 |
| 内容审核 | DeepSeek API + DFA 敏感词 | 双层内容安全检测 |
| API 文档 | Knife4j 4.5.0（OpenAPI 3） | 生成接口调试文档 |
| 构建工具 | Maven | 项目依赖管理与构建 |
| 部署 | Docker / PM2 + Nginx | Docker 容器化编排 + PM2 进程管理 |
| CI/CD | GitHub Actions | push 自动触发编译、测试、构建 |

---

## 六、数据库核心表设计

| 表名 | 说明 | 核心字段 |
|------|------|------|
| sys_user | 用户表 | id, username, password, real_name, role_id, class_id, avatar, phone, email, status, first_login |
| sys_role | 角色表 | id, role_name, role_code |
| sys_menu | 菜单权限表 | id, menu_name, parent_id, path, permission |
| sys_role_menu | 角色-菜单关联 | id, role_id, menu_id |
| sys_dict | 数据字典表 | id, dict_type, dict_label, dict_value, sort |
| sys_log | 操作日志表 | id, user_id, username, action, target, ip, request_method, request_path, duration, result |
| sys_notice | 公告表 | id, title, content, publisher_id, publish_time, top_flag, status |
| sys_notification | 消息通知表 | id, user_id, title, content, type（WORK_AUDIT/WORK_PUBLISH/SCORE/BATCH_NOTICE/SYSTEM）, ref_id, is_read |
| work | 作品主表 | id, title, summary, tech_stack, advisor, cover_url, video_url, preview_url, run_desc, status, submitter_id, submit_time, batch_id, view_count, like_count |
| work_member | 作品成员表 | id, work_id, student_id, student_name, student_no, class_name, is_leader |
| work_attachment | 作品附件表 | id, work_id（允许空）, file_name, file_type, file_size, file_url, category |
| work_audit | 作品审核表 | id, work_id, auditor_id, result, reason, audit_time |
| work_publish | 作品发布表 | id, work_id, publish_status, featured, preview_url, publish_time, offline_time |
| work_score | 作品评分表 | id, work_id, teacher_id, innovation, difficulty, completion, practicality, total, comment, batch_id |
| work_comment | 作品评论表 | id, work_id, user_id（允许空）, guest_name, content, parent_id |
| work_like | 作品点赞表 | id, work_id, user_id, create_time |
| work_tag | 作品标签关联表 | id, work_id, tag_id |
| tag | 标签表 | id, tag_name, create_time |
| score_batch | 评分批次表 | id, batch_name, start_time, end_time, class_scopes, status, rank_published, notice_title, notice_content |
| sensitive_rule | 内容审核规则表 | id, rule_name, system_prompt, enabled_categories, on_reject_action, status |
| rank_reward | 奖项配置表 | id, batch_id, reward_level, reward_name, prize_name, prize_image, quota |
| reward_issue | 奖品发放记录表 | id, reward_id, work_id, issue_status, issue_time, operator_id |
| reward_config | 奖品配置表 | id, batch_id, reward_level, reward_name, prize_name, quota |

> **work_member 成员说明**：`student_id` 关联 `sys_user.id`（成员在系统中有账号时填入，无账号可为空）；`student_name`、`student_no`、`class_name` 必填，确保未注册账号的团队成员也能被记录。

> **status 与 publish_status 职责分离**：`work.status` 仅管理审核流转（草稿→已提交→已驳回→已通过）；`work_publish.publish_status` 仅管理发布流转（已发布/已下线）。`work_publish.featured` 标记精选。两表通过 work_id 关联，职责互不交叉。

> **work_comment 游客评论**：`user_id` 允许为空，`guest_name` 记录游客昵称。`parent_id` 支持回复嵌套。

---

## 七、接口与页面建议

### 7.1 核心后端接口
- 登录认证接口（含首次登录检测）
- 用户信息查询与修改接口
- 邮箱验证码发送与注册接口
- 作品创建、编辑、提交、删除接口
- 作品列表、详情、搜索接口（含点赞/取消点赞）
- 审核通过/驳回接口
- 评分批次创建与配置接口（含通知内容配置）
- 评分提交与评分汇总接口
- 评论发布与评论列表接口（支持游客评论）
- 排行榜查询接口（支持批次筛选、技术栈分类）
- 排行榜公示/取消公示接口
- 公告管理接口
- 内容审核规则管理接口
- DeepSeek 连通性测试接口
- 消息通知查询与已读接口
- 精选作品与预览地址配置接口
- 奖品配置与发放接口
- AI 用户批量导入接口
- 评分批次通知发布接口

### 7.2 前端页面
- 登录页（含首次登录改密引导）
- 注册页（邮箱验证码注册）
- 学生端首页
- 作品提交页
- 我的作品页
- 我的评分与排名页
- 作品展示列表页（含搜索、筛选）
- 作品详情页（含点赞、运行说明折叠、视频播放）
- 教师评分页（匿名评审）
- 教师评分历史页
- 教师排行榜页
- 排行榜页（含批次切换、技术栈分类）
- 消息通知页
- 管理后台首页（ECharts 仪表盘）
- 审核管理页
- 评分批次管理页（含通知配置）
- 公告管理页
- 内容审核规则管理页
- 精选作品与预览地址管理能力
- 用户管理页（含 AI 导入入口）
- 奖品配置页
- 操作日志页
- 数据字典页
- 评论管理页
- 个人信息页（修改头像、联系方式、密码）

---

## 八、MVP 范围与迭代建议

### 8.1 P0 必做功能
- 登录认证与角色权限（含首次登录改密）
- 作品提交、编辑、删除、状态查询
- 作品审核、发布与前台展示
- 教师评分与评语
- LLM 内容安全审核与检测规则管理（含 DFA 敏感词辅助过滤）
- 评分批次管理（评分截止时间控制）
- 综合排行榜（含 Redis 缓存）
- 公告管理
- 消息通知（作品状态变更通知、排行公示通知）

### 8.2 P1 加分功能
- 评论互动（含游客评论、回复嵌套）
- 分类排行榜（按技术栈筛选）
- 操作日志
- 数据字典维护
- 奖品管理（前 N 名奖品配置与展示）
- 作品点赞
- 评分批次通知（含新学生自动补发）

### 8.3 P2 展示亮点功能
- 精选作品在线体验
- 服务器预览地址与 Nginx 代理配置管理
- 排行榜公示控制
- Redis 排行榜缓存与防穿透
- 图表统计页面（管理端 ECharts 仪表盘）
- AI 用户批量导入
- 深色模式
- 邮箱验证自助注册

---

## 九、验收标准建议

### 9.1 功能验收
- 学生/教师/管理员可成功登录，首次登录需修改密码
- 学生可创建作品、上传附件、填写运行说明、提交审核
- 管理员可完成审核并查看审核记录
- 教师可对通过审核的作品进行评分与填写评语
- 系统可根据评分结果生成排行榜，管理员可控制公示
- 前台可正常浏览作品列表、详情、视频、排行榜与评论
- 排行榜批次切换和技术栈分类筛选功能正常
- 内容审核规则可配置，LLM 审核正常拦截违规内容
- 消息通知在审核通过/驳回、发布、排行公示时自动发送

### 9.2 数据验收
- 用户、作品、评分、公告、评论等核心数据可正确入库
- 作品状态流转符合业务规则
- 排行榜计算结果与评分数据一致
- 点赞、浏览计数的读写一致性

### 9.3 演示验收
- 提供至少 1 个完整的学生提交流程演示
- 提供至少 1 个管理员审核流程演示
- 提供至少 1 个教师评分与排行榜更新演示
- 提供至少 1 个内容安全审核拦截演示
- 如实现精选在线体验，应提供可访问的预览地址

---

## 十、团队分工（4 人组）

| 角色 | 负责模块 | 输出 |
|------|------|------|
| 组长（全栈） | 后端架构、用户管理、权限认证、审核管理、LLM 内容安全审核、精选预览地址、前端页面设计 | 后端代码、SQL 脚本、Nginx 配置、前端页面 |
| 组员 1 | 集成测试、接口测试、冒烟测试 | 测试代码、测试报告 |
| 组员 2 | 单元测试、手工测试、安全测试 | 测试代码、安全测试报告 |
| 组员 3 | 性能测试、验收测试、文档编写 | 性能报告、验收报告、用户手册 |

---

## 十一、项目风险与应对

| 风险 | 说明 | 应对策略 |
|------|------|------|
| 功能范围过大 | 实训周期有限，容易后期来不及 | 明确 P0、P1、P2 范围，先完成核心功能 |
| 在线体验实现复杂 | 涉及 Nginx、运行环境和学生项目依赖 | 精选作品限量部署；平台只维护预览地址 |
| 团队协作冲突 | 前后端接口或字段不统一 | 提前制定接口文档与命名规范 |
| 数据准备不足 | 演示时缺少测试数据 | 提前准备学生、教师、作品、评分样例数据 |
| LLM 接口不稳定 | 网络波动或第三方接口异常会影响内容审核 | 设置超时、重试与人工复核兜底策略 |

---

> 本文档为 V2.0，基于 V1.9 同步当前项目实际代码状态。新增邮箱验证注册、AI 用户导入、点赞、游客评论、运行说明折叠展示、排行榜公示控制、评分批次通知、深色模式等功能。技术栈对齐实现（Vite 8 / Redis 已落地），数据库表结构同步至最新迁移（含 9 个增量迁移文件）。
